name: 👀 Run Checks

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  checks:
    name: Calling checks workflow
    if: github.actor != 'shoptet-addon-repository[bot]'
    uses: shoptet/addon-repository-actions-config/.github/workflows/checks.workflow.yml@main